class Permissoes {
    constructor(permissionsId, profileName, profileDescription,permissionsList) {
        this.idPermissoes = permissionsId;
        this.nomePerfil = profileName;
        this.descricaoPerfil = profileDescription;
        this.listaPermissoes = permissionsList;
    }
}